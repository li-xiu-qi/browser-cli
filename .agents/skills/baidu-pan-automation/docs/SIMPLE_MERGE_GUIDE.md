# 极简合并指南（懒人版）

> 目标：一次性完成 90% 的合并，只需 10 分钟

---

## 🎯 一句话总结

**删除这 3 个**（完全重复）：
- `books`, `models`, `drawio`

**移动这些到 `我的资源/A-Resources/`**（独有内容）：
- 根目录：`公众号二维码`, `大模型课件.rar`
- `图片/` 里的所有文件（4个）
- `安装包/` 里的所有文件（82个）

**剩下 2 个看一眼告诉我**：
- `courses/`, `obsidian/`（看起来一样就删，不一样就告诉我）

---

## 📱 手机/网页操作步骤

### 第 1 步：删除重复文件夹（2分钟）

打开 https://pan.baidu.com

```
路径：来自：本地电脑 → A-Resources
选中：books、models、drawio（按住Ctrl多选）
操作：删除 → 确认
```

---

### 第 2 步：移动独有内容（5分钟）

**A. 根目录的 2 个项目**
```
在同一页面选中：公众号二维码、大模型课件.rar
点击：移动到 → 我的资源 → A-Resources → 确认
```

**B. 图片文件夹的内容**
```
进入：来自：本地电脑 → A-Resources → 图片/
全选（Ctrl+A）
点击：移动到 → 我的资源 → A-Resources → 图片/ → 确认
如果提示"文件已存在"，选"保留两者"
```

**C. 安装包文件夹的内容**
```
同上操作，把安装包里的所有文件移动到目标
```

---

### 第 3 步：处理最后 2 个（3分钟）

**courses/** 和 **obsidian/**

```
打开两个窗口对比：
- 窗口1：我的资源 → A-Resources → courses
- 窗口2：来自：本地电脑 → A-Resources → courses

看一眼：
• 如果看起来一样（都有相同的子文件夹）→ 删除本地电脑版
• 如果看起来不一样 → 截图发给我

obsidian/ 同理
```

---

### 第 4 步：删除空目录（1分钟）

```
检查：来自：本地电脑 → A-Resources/ 是否已空
如果是：删除整个 A-Resources 文件夹
```

---

## ✅ 完成后检查

你的 `我的资源/A-Resources/` 应该包含：
- ✅ 原来的所有内容
- ✅ 新增的：公众号二维码、大模型课件
- ✅ 图片/ 里多了4个文件
- ✅ 安装包/ 里多了82个文件
- ✅ courses/ 和 obsidian/（根据你的判断保留或合并）

`来自：本地电脑/A-Resources/` 应该：
- ✅ 已删除或已空

---

## 🆘 遇到问题？

| 问题 | 解决 |
|------|------|
| 移动时提示"文件已存在" | 选"保留两者" |
| 删除后想恢复 | 去回收站还原（10天内） |
| 不确定要不要删 | 先移动到一个临时文件夹，观察几天 |
| 操作到一半网断了 | 刷新页面，已完成的不用重复操作 |

---

**就这些！10 分钟后告诉我完成情况。**
